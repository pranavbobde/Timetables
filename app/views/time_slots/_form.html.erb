<%= form_with(model: time_slot, local: true) do |form| %>
  <% if time_slot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(time_slot.errors.count, "error") %> prohibited this time_slot from being saved:</h2>

      <ul>
      <% time_slot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :"Please select the date for the booking" %>
    <%= form.date_select :Date %>
  </div>

  <div class="field">
    <%= form.label :"Please enter the start time" %>
    <%= form.time_select :StartTime %>
  </div>

  <div class="field">
    <%= form.label :"Please enter the end time" %>
    <%= form.time_select :EndTime %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
