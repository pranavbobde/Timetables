<%= form_with(model: room, local: true) do |form| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :floor %>
    <%= form.text_field :floor %>
  </div>

  <div class="field">
    <%= form.label :seating %>
    <%= form.text_field :seating %>
  </div>

  <div class="field">
    Projector:
    <%= check_box_tag 'room[projector]', 'projector' %>
  </div>
  
  <div class="field">
    Computers:
    <%= check_box_tag 'room[computers]', 'computers' %>
  </div>
  
  <div class="field">
    Audio Enhancements:
    <%= check_box_tag 'room[Audio_enhancements]', 'Audio_enhancements' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
  
  
<% end %>
